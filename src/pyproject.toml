[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "graphs_sriveram"
version = "0.1.0"
description = "A Python library that implements Dijkstra's shortest path algorithm."
requires-python = ">=3.9"

[tool.setuptools]
packages = ["graphs_sriveram"]